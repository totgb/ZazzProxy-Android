package com.totgb.zazzproxy.network;

/** File-transfer protocol limits, kept separate from UDP datagram limits. */
final class FileTransfer {
    static final int CHUNK_BYTES = 800;

    private FileTransfer() { }
}
